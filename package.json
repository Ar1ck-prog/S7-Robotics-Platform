{
  "name": "s7-robotics-platform",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check core.js && node --check app.js"
  }
}
